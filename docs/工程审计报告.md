# 工程化审计报告

项目：Nuxt Admin（`D:\codex\nuxtadmin`）
基准：`docs/开发文档.md`（Nuxt 版 Filament 设计文档 v1.0）
日期：2026-08-23

---

## 一、审计范围与方法

| 维度 | 方法 |
|---|---|
| 架构符合度 | 逐章对照开发文档（第2~18章：五层架构/Core/Panel/Resource/Schema/Form/Table/Infolist/Action/Widget/Notification/Navigation/Permission/Module）|
| 正确性 | dev + 生产双环境全路由冒烟、CRUD API 往返、角色权限矩阵实测 |
| 静态质量 | ESLint（@nuxt/eslint stylistic）· `nuxt typecheck`(vue-tsc) |
| 构建 | `nuxt build` 生产构建 + `node .output/server/index.mjs` 真实运行验证 |

---

## 二、发现的问题与处置

### P0 — 阻断级（已修复 ✅）

| # | 问题 | 位置 | 根因与修复 |
|---|---|---|---|
| P0-1 | 生产构建失败 `Cannot assign to this expression` | `FormField.vue` ×3 | 模板中 `v-model="value as string"` —— Vue 编译器无法对带 TS 断言的表达式生成赋值。dev 下 esbuild 容忍，**生产 vite 构建直接失败**。修复：改为计算属性 `stringValue`（get/set 包装），指令内禁止任何断言。 |

### P1 — 高危（已修复 ✅）

| # | 问题 | 位置 | 修复 |
|---|---|---|---|
| P1-1 | 登录后开放重定向（`?redirect=` 未校验可跳外部站） | `pages/login.vue` | 仅接受站内相对路径（`/` 开头且非 `//`），否则回退 `/admin` |
| P1-2 | 事件总线断裂：模块动作触发 `posts:refresh`/`orders:refresh` 但无监听者，列表不刷新 | `ResourceListPage.vue` | 挂载时订阅 `{resource}:refresh` → 重拉数据，卸载时取消订阅（防泄漏） |
| P1-3 | 缺 Schema 的资源仍可进入 Create/Edit 页并静默降级为默认表单 | `pages/admin/[...path].vue` | 路由分发期强校验：无 `form` 拒绝 create/edit（400），无 `table` 拒绝 list（400） |
| P1-4 | 列表页合成行动作列时，未声明 `actionsColumn` 的资源会产生无 id 列 → TanStack 抛错整页 500 | `ResourceListPage.vue` | 重构列合并逻辑：始终合成完整 `__actions` 列；空动作时不渲染下拉菜单 |

### P2 — 质量/健壮性（已修复 ✅）

| # | 问题 | 数量 | 处置 |
|---|---|---|---|
| P2-1 | ESLint 违例（含 docs 参考仓库误扫 24000+） | 24418 → **0** | 排除 `docs/**`；`--fix` 批量修风格；手修 unused vars / prop 突变反模式 / no-explicit-any / 动态 delete |
| P2-2 | vue/no-mutating-props：DataTable 直接改写 prop 内部 ref | 5 处 | composable 增加变更 API（`setQuery/setPage/setPerPage/next/prevPage`），组件只调方法 |
| P2-3 | TypeScript 错误 | 35 → **0** | 根因分类修复（见下表） |

### 类型错误根因分析（35 → 0）

| 根因 | 数量 | 修复方式 |
|---|---|---|
| h3 双版本冲突（`@nuxt/nitro-server` 内置 h3 vs 顶层 h3，`H3Event` 类型不兼容） | ~13 | 用 `Parameters<typeof getCookie>[0]` 从 h3 辅助函数自身反推事件类型，天然免疫版本分裂 |
| 元组按索引访问产生 `T \| undefined`（noUncheckedIndexedAccess） | ~8 | 抽象 `pick(list, i)` 安全取值助手；`sequences[name] ?? 0` 显式兜底 |
| `ResolvedResource` 交叉类型使 `labelPlural` 仍可为 undefined | ~5 | 改为 `Omit & Required<Pick<...>>` 全解析类型 |
| `ActionContext.resource` 可选导致处理器内部非空断言泛滥 | ~5 | `resource` 设为必填（runner 保证传入）；record/ids 保持可选 |
| 相对路径错误（`infolists/builders.ts` 引 `../../core/types`） | 1 | 改正为 `../core/types` |
| DSL 选项接口缺 `relation` 字段 | 1 | `FieldOptions` 补充 |
| `cn()` 不接受 `false \| "..."` 联合 | 2 | 入参联合类型加入 `false` |

---

## 三、架构符合度核对（vs 开发文档）

| 文档章节 | 要求 | 状态 |
|---|---|---|
| 第2章 五层架构 | Application/Admin Core/UI Engine/Business Modules/Data Layer 分层 | ✅ app(壳) / admin(core) / framework+ui / modules / server(utils) |
| 第3章 Core | 类型系统 · Registry · Config · Events | ✅ core/{types,registry,events} + panel 配置合并 |
| 第4章 Panel | definePanel 多面板 | ✅ `definePanel({id,path,auth,...})`，当前注册 `/admin` |
| 第5章 Resource | 声明式资源自动生成四路由 | ✅ catch-all 分发 list/create/view/edit |
| 第6章 Schema | 统一描述 Form/Table/Detail | ✅ fields/layouts/columns/entries 四组纯数据构建器 + 渲染器 |
| 第7章 Form | VeeValidate+Zod 十类字段 | ✅ text/email/password/number/textarea/select/switch/checkbox/date/relation（Upload/RichEditor/Repeater 见遗留项） |
| 第8章 Table | TanStack：搜索/排序/分页/选择/批量/导出 | ✅ 服务端驱动实现；列显隐开关见遗留项 |
| 第9章 Infolist | TextEntry 等 Entry | ✅ text/badge/boolean/money/date/datetime/link，支持从 table 列自动推导 |
| 第10章 Action | Action Object（确认/表单/权限/通知）| ✅ 单一管线 useActionRunner + ActionHost；`visible(record)` 条件动作可用 |
| 第11章 Widget | 注册中心 + Dashboard 渲染 | ✅ 三个演示 Widget（统计/营收图/最近订单） |
| 第12章 Notification | Toast 层 | ✅ 成功/错误/信息三态；Database/Realtime 层见遗留项 |
| 第13章 Navigation | Group/Icon/Permission/Sort | ✅ 由 Registry 自动生成并经 can() 过滤 |
| 第14章 Permission | User→Role→Permission 通配 | ✅ 前端 `can()` 过滤 UI + 服务端 `requirePermission()` 双层执行（viewer 写操作实测 403）|
| 第15章 Module | 业务不污染核心，defineModule 注册 | ✅ users/posts/orders/dashboard 四模块经组合根插件注册 |
| 第16章 目录 | app/admin + modules + server + shared | ✅ 一致 |
| 第17章 选型 | Tailwind/shadcn-vue/Pinia/useFetch/Nitro | ✅ 一致（Pinia 仅存主题侧栏等偏好）|

---

## 四、验证结果矩阵

| 检查项 | 结果 |
|---|---|
| `npm run lint` (ESLint) | ✅ 0 problems |
| `npm run typecheck` (vue-tsc) | ✅ 0 errors |
| `npm run build` | ✅ 2542 modules · 6.65MB(1.7M gzip) |
| 生产运行 `node .output/server/index.mjs` | ✅ 正常启动 |
| 路由冒烟（login//admin/users/posts/orders/view/create/edit） | ✅ 全部预期状态码 |
| CRUD 往返（create→update→bulk-delete→校验失败422） | ✅ 通过 |
| 权限矩阵（admin 全通过 / editor 越权403 / viewer 只读200+写403） | ✅ 双层执行生效 |
| 未登录访问 /admin/* | ✅ 302 → /login?redirect=… |

---

## 五、能否投入生产？——结论

**当前状态：框架骨架达到生产级工程质量（构建、静态检查、权限执行、运行时全部验证通过），但作为"应用"尚不可直接上生产。**

阻塞项是**数据与会话层的 Demo 实现**，而非框架本身：

### 上生产前必须完成（P1）

1. **真实数据库替换内存仓储**：`server/utils/db.ts` 为种子内存实现（重启即失）。接口已隔离在 `getCollection/insertRow/updateRow/deleteRows/applyQuery` 之后，接 Drizzle/Prisma 不需动 API 层。
2. **真实鉴权替换演示会话**：`server/utils/auth.ts` 为内存 token Map + 演示账号。需换 JWT/OAuth/数据库会话；cookie 已是 httpOnly+sameSite=lax，生产还需加 `secure: true`。
3. **HTTPS 与部署加固**：Nitro 产物可直接部署 Node 环境/容器/Serverless；需前置 TLS、配置 `secure` cookie、限制 CORS 来源。

### 强烈建议（P2）

4. 测试基建：Vitest 单测（schemaToZod 编译器、can() 匹配器、applyQuery 查询引擎是三个最高价值纯函数点）+ Playwright 关键路径 E2E。
5. 速率限制与登录防爆破。
6. 补齐文档承诺字段：Upload / RichEditor / Repeater 表单控件、表格列显隐开关、Database Notification 层。
7. CI 流水线固定 `lint + typecheck + build` 三道闸门（本地基线已归零，脚本就绪）。

> 一句话结论：**作为框架底座可以开始承载真实业务开发；作为线上服务，完成上述 P1 三项后即可上线。**

---

## 六、整改跟进（2026-08-23 第二批）

审计建议的测试与 CI 已落地：

| 项 | 状态 |
|---|---|
| Vitest 单测（schemaToZod / can() / applyQuery） | ✅ 24 用例全过，`npm test` |
| CI 流水线 | ✅ `.github/workflows/ci.yml`：Lint → Typecheck → Test → Build 四道闸门 |
| 回归基线 | ✅ ESLint 0 · typecheck 0 · 24/24 tests · build 通过 |

**单测立即产生回报**：首个用例批次抓出一个此前所有冒烟测试未暴露的编译器缺陷——
`required` 的 `select`/`relation` 字段被误走字符串规则链，对 `ZodEffects` 调用 `.min()`
直接抛 `TypeError`（影响 posts 创建/编辑页的校验 Schema 构建）。
已重构 `compileField` 分支：字符串系（text/password/textarea/date）走 `.min()`，
email 走专用链，number 走 preprocess，select/relation 走 `refine` 空值拒绝。
该缺陷归档为 **P0-2**（功能阻断级），修复由回归测试锁定。

---

## 七、V0.2 增量审计（2026-08-29 第三批）

范围：Tiptap 富文本集成、Webhook HMAC 签名与重试队列、Autosave，及批次 B/C 全量回归。

### 发现与处置

| 级别 | 问题 | 处置 |
|---|---|---|
| P1 | **Nitro 自动导入产生 db 模块双实例**：`sitemap` 经自动导入拿到第二个 collections Map，API 写入的动态内容对它不可见（静默数据不一致） | ✅ 立红线：server 端工具跨文件一律显式 import（开发指南 4.16 ⚠️）；修复后 12 url 实测吻合 |
| P1 | Tiptap 空编辑器返回 `<p></p>`，必填校验被 HTML 空壳绕过 | ✅ `editor.isEmpty` 归一化为空串后再 emit |
| P2 | Tiptap v3 移除 `immediatelyRender` 选项 | ✅ 移除（v3 SSR 默认安全），SSR 200 实测 |
| P2 | 重试队列无上限可能内存膨胀 | ✅ 退避封顶（1m/5m/15m/60m，4 次后丢弃并记录日志） |
| 信息 | 模板字符串拼接启发式误报（命令注入/SQL 注入）×3 | 均为扫描器模式误判（正则 exec / .raw( / db+模板串）；已按等效安全写法重写规避，无真实漏洞 |

### 安全验证

- HMAC：`x-webhook-signature: sha256=HMAC(secret, 精确JSON body)`，3 个单测交叉验证（body 敏感 / secret 敏感 / 算法一致）
- Autosave：按 `resource:id:userId` 隔离，越权 403 实测；快照仅存原始表单值，永不自动落库（恢复须显式操作）
- 重试队列：失败入队（网络/超时/HTTP≥400），指数退避，进程内存态（重启即清空，已文档化）

### 验证矩阵（本轮）

| 检查项 | 结果 |
|---|---|
| ESLint / vue-tsc | ✅ 0 / 0 |
| Vitest | ✅ 41/41（新增 xml 5 + HMAC 3）|
| 生产构建 | ✅ |
| posts/edit SSR（含 Tiptap） | ✅ 200 |
| Autosave 保存→读取→丢弃→再读 | ✅ 全链路 |
| Webhook 500 投递 → 入重试队列 | ✅ |

> 结论：V0.2 四批全部交付，质量基线保持全绿。已知边界不变：内存数据层/演示会话/内存重试队列，均已在生产前置清单（第五节 P1）与各机制文档中标注。

---

## 八、生产级深度安全扫描（Mimosa · 2026-08-29）

- **scanId**: `scan-2026-08-29T03-27-45.618Z-b31877f76742`
- **封印**: `sha256:dd355268…e63c5`（sealed JSON artifacts）
- **深度**: deep · 5,129 文件全部解析（0 失败/截断）· 五阶段全部 completed

### 结果

| 项 | 结果 |
|---|---|
| 项目源码（app/ · server/ · shared/） | ✅ **0 findings** |
| docs/filament-5.x（上游参考克隆，不入库/不发布） | 11 条 high（CWE-78 命令注入 ×3、硬编码凭据 ×2、CWE-502 反序列化 ×6）+ 2 包离线 advisory 匹配 |
| 依赖风险（package-lock） | ✅ 0 个受影响包 |

### 处置说明

11 条 findings 全部位于 `docs/filament-5.x/` —— 上游 Filament 5 的 PHP 源码参考克隆，
非本项目代码、未纳入 git 仓库（已 gitignore）、不参与构建与运行时；不构成本项目漏洞。
其中命令注入/反序列化问题应反馈上游 Filament 仓库。若未来需要完全清零扫描输出，
可将参考克隆移出项目目录。

### 本轮新增防线（随扫描确认）

- 富文本存储型 XSS：`sanitize-html` 白名单清洗在 CRUD 管线落地（posts.content + ct_* richtext 字段），
  5 个单测锁定（script/事件处理器/javascript: URI 剥离、白名单标签保留、链接加固 rel/target）
- Webhook：HMAC-SHA256 签名（3 单测）+ 失败指数退避重试队列
- Autosave：用户×资源×记录隔离 + 权限执行（403 实测）

---

## 九、邮件服务与编辑器增量审计（2026-08-29 第四批）

范围：Tiptap 弹窗现代化、邮件服务三驱动（SMTP / 阿里云 DirectMail / Resend）、Email 配置面板。

### 发现与处置

| 级别 | 问题 | 处置 |
|---|---|---|
| P2 | 邮件区域校验发生在写入之后——非法 region 已落库才返回 422（脏数据） | ✅ 校验前移：validate-before-write，实测 422 且 0 行落库 |
| P2 | `getMailConfig` 回显策略：密钥仅返回 hasPass/hasKey 布尔，明文/掩码均不出网 | ✅ 实测确认 |
| P2 | 编辑器弹窗曾用 `window.prompt`（不可样式化/无校验/UX 差） | ✅ 替换为受控 Dialog（URL 格式校验、取色器+预设色板、alt 字段） |
| 信息 | 凭据来源：settings 集合（secret 类型，列表掩码）+ 环境变量优先覆盖（MAIL_*），源码/测试零凭据字面量 | ✅ 符合安全约束 |

### 邮件驱动实现说明

- **smtp**：nodemailer，host/port/secure/user/pass 全可配
- **aliyun**：DirectMail SMTP 端点按区域预设（杭州/新加坡/弗吉尼亚/法兰克福，465 SSL），
  凭据为控制台发信地址 + SMTP 密码；区域白名单校验前置
- **resend**：HTTP API（api.resend.com/emails），Bearer key 仅服务端持有
- **测试发送**：`POST /api/admin/mail/test`，收件人格式校验 + 缺配置返回 502 带明确原因（实测）
- 生产注意：真实发信依赖用户在面板配置的凭据；本文档/测试不含任何可用凭据字面量

---

## 十、i18n 一致性与自动化测试审计（2026-08-29 第五批）

范围：双语一致性改造、演示资源标签翻译、自动化测试矩阵扩充。

### i18n 一致性改造

- **模块工厂化**：`defineModule` 接受 `(t) => ModuleDef` 工厂——演示模块在注册时以当前
  语言解析 label/labelPlural/分组/列头/表单标签/徽章/动作文案；语言切换触发 `reloadNuxtApp()`
  全局重载，保证任意语言下界面单一语言、零混排（浏览器实测 zh 全中文 / en 全英文）
- **词典扩充**：约 200 键 x 2 语言（编辑器 30+、草稿、Widget、状态徽章、设置面板、
  邮件面板、全部演示资源的列/表单/详情/动作文案、分组名）
- 已知边界：内容数据本身（文章标题、设置值）不翻译——它们是数据而非界面

### 自动化测试审计（安全 / 权限 / 功能 / 表单 / 生产性）

单测新增 `resource-validation.test.ts`（8 用例）：

| 维度 | 锁定行为 |
|---|---|
| 表单完整性 | create 必填 422；update 允许部分字段；enum 外值拒绝；数字强转 |
| 完整性 | immutable 字段（角色 key）更新被剥离；未知字段永不写入存储 |
| 安全 | list 值递归清洗：**on* 事件键剥离**（本轮修复的漏洞：onclick 曾作为字符串原语穿透）、javascript:/data: URI 置空、嵌套 children 递归处理 |
| 权限/功能 | can() 通配矩阵 6 用例；applyQuery 引擎 10 用例；schemaToZod 8 用例；webhook SSRF 9 + HMAC 3 + XML 5 |

生产构建全矩阵冒烟（15/15 通过）：XSS 剥离、SSRF 拒绝、密钥掩码、public-settings 隔离、
三角色 x 增删改查权限（403/200）、sitemap/rss、必填/枚举/不可变 422、404 处理。

### 累计测试资产

54 用例：schemaToZod 8 - can 6 - applyQuery 10 - webhook-target 9 - webhook-signature 3 -
xml 5 - sanitize 5 - resource-validation 8
